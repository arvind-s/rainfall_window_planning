# Runtime dependencies for the Streamlit app (Streamlit Cloud reads this file).
# The app only reads the baked data/out/ files -- it does NOT download rainfall,
# so imdlib / FTP libs are not needed here (see requirements-build.txt for that).
streamlit>=1.40
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
geopandas>=0.14
shapely>=2.0
folium>=0.15
streamlit-folium>=0.18
plotly>=5.18
branca>=0.7
